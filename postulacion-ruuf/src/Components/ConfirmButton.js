// ConfirmButton.js
import React from 'react';

const ConfirmButton = ({ onClick }) => {
    return (
        <button onClick={onClick}>Confirmar Valores</button>
    );
};

export default ConfirmButton;
